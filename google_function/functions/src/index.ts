require('dotenv').config();
/*import * as functions from 'firebase-functions';
import axios from 'axios'*/

/*
const url : string = process.env.URL || '';
exports.createSubTenants = functions.firestore.document('SubTenants/{id}')
  .onWrite((event: functions.Change<functions.firestore.DocumentSnapshot>, context: functions.EventContext) => {
    try {
      axios.post(`${url+'sub-admin/create'}`, event.after.data())
        .then(function (response) {
          return true;
        });
    } catch (e) {
      console.log("Error", e)
    }
  });*/
